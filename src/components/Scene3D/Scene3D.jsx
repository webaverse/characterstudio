import styled from "styled-components"
import Viewer3D from "components/Viewer3D"

export default function Scene3D() {
  return <Holder>{/* <Viewer3D /> */}</Holder>
}

const Holder = styled.div`
  flex: 1;
  height: 100%;
`
